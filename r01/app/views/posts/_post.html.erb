<div id="<%= dom_id post %>">
  <p>
    <strong>Content:</strong>
    <%= post.content %>
  </p>

  <p>
    <strong>Image:</strong>
    <% if post.image.present? %>
      <%= image_tag post.image.url %>
    <% end %>
  </p>
</div>
